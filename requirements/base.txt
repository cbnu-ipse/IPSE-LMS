# ==========================================
# Core
# ==========================================
python-decouple>=3.8  # .env 환경변수 관리
pytz>=2024.1
Pillow>=10.2.0  # 이미지 업로드 처리

# ==========================================
# Django Framework
# ==========================================
Django>=5.0,<6.0  # 장고 5.x 최신 버전으로 업그레이드!
django-model-utils>=4.3.1
django-filter>=24.1

# ==========================================
# Real-time (Django Channels + WebSocket)
# ==========================================
channels>=4.0.0       # Django WebSocket / ASGI 채널 레이어
channels-redis>=4.0.0 # 멀티 프로세스 프로덕션용 Redis 채널 레이어
daphne>=4.0.0         # Django Channels 공식 ASGI 서버 (gunicorn 대체)

# ==========================================
# Frontend & UI (IPSE 커스텀)
# ==========================================
whitenoise>=6.6.0  # 정적 파일 서빙
django-tailwind>=3.8.0  # 🌟 Tailwind CSS 연동을 위한 필수 패키지

# ==========================================
# API & REST (추후 기능 확장을 위해 유지)
# ==========================================
djangorestframework>=3.15.0
django-cors-headers>=4.3.1
icalendar==7.1.1
django-dbbackup>=4.2.1
pywebpush>=1.14.0
beautifulsoup4>=4.12.3


