# ==========================================
# IPSE LMS — judge(contest/problems) 전용 DB Docker 환경변수 템플릿
# 이 파일을 .env.judge.docker 로 복사한 후 값을 채우세요.
# .env.judge.docker 는 .gitignore에 등록되어 있습니다.
# ==========================================

POSTGRES_DB=beta_judge
POSTGRES_USER=ipse_judge
POSTGRES_PASSWORD=change-this-strong-password
